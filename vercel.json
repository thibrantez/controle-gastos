{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "env": {
    "ANTHROPIC_API_KEY": "Obter em console.anthropic.com → API Keys → configurar no Vercel Dashboard em Settings › Environment Variables",
    "GEMINI_API_KEY": "Obter em aistudio.google.com → API Keys → configurar no Vercel Dashboard em Settings › Environment Variables"
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options",        "value": "DENY" },
        { "key": "X-XSS-Protection",       "value": "1; mode=block" },
        { "key": "Referrer-Policy",         "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
